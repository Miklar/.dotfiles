---
description: Add or update an entry in the private Pi work log
argument-hint: "<what happened>"
---

Use the `daily-standup` skill to update today's private work log at `~/.pi/agent/work-log/YYYY-MM-DD.md`.

Log this work or status update:

$ARGUMENTS

Keep it concise, put it in the right section, merge with related entries if today's log already contains the same task, and avoid secrets or sensitive details.
