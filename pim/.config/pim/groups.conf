# PIM subscription groups for pim-activate.sh / morning-pim.sh
#
# Format: <group>=<subscription>[,<subscription>...]
# Each <subscription> can be a subscription name or a subscription ID
# (anything `az account show --subscription <x>` accepts).
#
# Lines starting with # and blank lines are ignored.

test=epiroc-digitalprogram-test,epiroc-evrypart-test
prod=REPLACE_WITH_PROD_SUBSCRIPTION_NAME_OR_ID
